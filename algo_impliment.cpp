#include<bits/stdc++.h>
using namespace std;
void mergen(vector<int>&a,int m,vector<int>&b,int n){
   vector<int>c;
   c.reserve(m+n);
   
}
int main (){
   vector<int>a,b;
   int m,n;
   cout<<"Enter first array element ";
   cin>>m;
   for(int i=0;i<m;i++){
      cin>>a[i];
   }
   cout<<"enter element of second array ";
   cin>>n;
   for(int =j0;j<m;j++){
      cin>>b[j]
   }
   merge(*a,m,*b,n);
   
}